# ========== Google SSO (obligatoire pour « Continuer avec Google ») ==========
# 1) Backend (Railway / serveur API) : GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, JWT_SECRET, CORS_ORIGINS
# 2) Google Cloud Console : Authorized redirect URIs = exactement l’URL ci‑dessous (sans slash final)
# 3) Landing (ce fichier) : VITE_UWI_API_BASE_URL + VITE_GOOGLE_REDIRECT_URI

# Backend API — URL de l’API (ex. Railway). Sans ça : « Backend non configuré »
VITE_UWI_API_BASE_URL=https://api.uwiapp.com
# Ex. Railway : https://xxx.up.railway.app

# Google SSO : URI de redirection (identique à celle configurée dans Google Console)
VITE_GOOGLE_REDIRECT_URI=https://www.uwiapp.com/auth/google/callback

# URL du dashboard client (si différent de l’origine, ex. admin sur admin.uwiapp.com, client sur uwiapp.com)
# VITE_UWI_APP_URL=https://uwiapp.com

# Optionnel : activer UI admin (P0 interne)
# VITE_ADMIN_UI_ENABLED=true

# Stripe Checkout (embedded) — page /checkout sur uwiapp.com
# Clé publique (pk_test_... ou pk_live_...) — visible côté client
# VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...
# URL du serveur Stripe : en dev http://localhost:4242 ; en prod l’URL du déploiement stripe-server (ex. https://uwi-stripe.railway.app)
# VITE_STRIPE_API_URL=http://localhost:4242
# Prod exemple : VITE_STRIPE_API_URL=https://uwi-stripe.railway.app
