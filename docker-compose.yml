version: "3.9"

services:
  agent:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - agent_db:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

volumes:
  agent_db:
